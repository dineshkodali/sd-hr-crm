import React, { useState, useEffect, useMemo } from 'react';
import axios from 'axios';
import {
 Download,
 Users,
 Building2,
 Bed,
 TrendingUp,
 PieChart,
 Home,
 BarChart3,
 Calendar
} from 'lucide-react';
import { generatePDF } from "../utils/pdfGenerator";
import { generateCSV } from "../utils/csvGenerator";

export default function ServiceUserAnalytics() {
 const [loading, setLoading] = useState(true);
 const [activeTab, setActiveTab] = useState('demographics');
 const [serviceUsers, setServiceUsers] = useState([]);
 const [properties, setProperties] = useState([]);
 const [rooms, setRooms] = useState([]);

 const [demographics, setDemographics] = useState({
 totalUsers: 0,
 activeUsers: 0,
 movedOut: 0,
 ageGroups: [],
 genderDistribution: [],
 immigrationStatus: [],
 nationalityDistribution: []
 });

 const [accommodation, setAccommodation] = useState({
 totalProperties: 0,
 totalRooms: 0,
 occupiedRooms: 0,
 occupancyRate: 0,
 propertyOccupancy: [],
 roomTypes: []
 });

 // Calculate age from DOB
 const calculateAge = (dob) => {
 if (!dob) return 0;
 const diff = Date.now() - new Date(dob).getTime();
 const age = new Date(diff).getUTCFullYear() - 1970;
 return Math.abs(age);
 };

 useEffect(() => {
 const fetchData = async () => {
 try {
 setLoading(true);
 // Determine API URL (using same logic as other components)
 let apiUrl = 'http://localhost:4000/api';
 if (import.meta.env.VITE_API_URL) apiUrl = import.meta.env.VITE_API_URL;
 else if (typeof window !== 'undefined') {
 if (window.location.hostname !== 'localhost' && window.location.hostname !== '127.0.0.1') {
 apiUrl = `${window.location.origin}/api`;
 }
 }

 const api = axios.create({ baseURL: apiUrl, withCredentials: true });

 const [usersRes, hotelsRes] = await Promise.all([
 api.get('/su/users'),
 api.get('/hotels')
 ]);

 const usersData = Array.isArray(usersRes.data) ? usersRes.data : (usersRes.data.users || []);
 const hotelsData = Array.isArray(hotelsRes.data) ? hotelsRes.data : (hotelsRes.data.data || []);

 setServiceUsers(usersData);
 setProperties(hotelsData);

 // Fetch rooms for all hotels to calculate occupancy correctly
 // Note: In a real app with many hotels, this might need optimization
 let allRooms = [];
 // fetch rooms for each hotel? Or is there a bulk endpoint?
 // Assuming we need to iterate or hit a specific endpoint. 
 // For now, let's try to fetch rooms if possible, or skip if too heavy.
 // If we look at ServiceUsersList, it fetches rooms per hotel.
 // Let's assume we can get basic stats from users+hotels for now, 
 // or try to fetch rooms for the first few or all if count is low.
 // Use a simpler approach: Process user data for demographics first.

 // --- Process Demographics ---
 const activeUsers = usersData.filter(u => u.status === 'Active');
 const movedOut = usersData.filter(u => u.status === 'Moved Out');

 // Age Groups
 const ages = activeUsers.map(u => calculateAge(u.date_of_birth || u.dob));
 const ageGroups = [
 { name: '18-25', value: ages.filter(a => a >= 18 && a <= 25).length },
 { name: '26-35', value: ages.filter(a => a >= 26 && a <= 35).length },
 { name: '36-50', value: ages.filter(a => a >= 36 && a <= 50).length },
 { name: '50+', value: ages.filter(a => a > 50).length }
 ];

 // Gender
 const genderCounts = {};
 activeUsers.forEach(u => {
 const g = u.gender || 'Unknown';
 genderCounts[g] = (genderCounts[g] || 0) + 1;
 });
 const genderDist = Object.entries(genderCounts).map(([name, value]) => ({ name, value }));

 // Immigration
 const immCounts = {};
 activeUsers.forEach(u => {
 const s = u.immigration_status || 'Unknown';
 immCounts[s] = (immCounts[s] || 0) + 1;
 });
 const immStatus = Object.entries(immCounts).map(([name, value]) => ({ name, value }));

 // Nationalities (Top 5)
 const natCounts = {};
 activeUsers.forEach(u => {
 const n = u.nationality || 'Unknown';
 natCounts[n] = (natCounts[n] || 0) + 1;
 });
 const natDist = Object.entries(natCounts)
 .map(([name, value]) => ({ name, value }))
 .sort((a, b) => b.value - a.value)
 .slice(0, 10); // Show top 10

 setDemographics({
 totalUsers: usersData.length,
 activeUsers: activeUsers.length,
 movedOut: movedOut.length,
 ageGroups,
 genderDistribution: genderDist,
 immigrationStatus: immStatus,
 nationalityDistribution: natDist
 });

 // --- Process Accommodation ---
 // We'll approximate occupancy based on active users vs 'implied' capacity 
 // OR if we can fetch rooms. Let's try to fetch rooms for all hotels properly.
 const roomPromises = hotelsData.map(h => api.get(`/su/rooms/${h.id || h.hotel_id}`).catch(() => ({ data: [] })));
 const roomsResponses = await Promise.all(roomPromises);

 let totalRoomsCount = 0;
 let occupiedRoomsCount = 0; // or count occupied status from rooms
 const propertyStats = [];

 roomsResponses.forEach((res, idx) => {
 const hotel = hotelsData[idx];
 let hotelRooms = [];
 if (Array.isArray(res.data)) hotelRooms = res.data;
 else if (Array.isArray(res.data?.rooms)) hotelRooms = res.data.rooms;
 else if (Array.isArray(res.data?.data)) hotelRooms = res.data.data;

 allRooms = [...allRooms, ...hotelRooms];

 const total = hotelRooms.length;
 // Count occupied by checking room status OR checking how many users are assigned
 // Strategy: Check room.status
 const occupied = hotelRooms.filter(r => r.status === 'Occupied' || r.is_occupied).length;

 totalRoomsCount += total;
 occupiedRoomsCount += occupied;

 if (total > 0) {
 propertyStats.push({
 name: hotel.name || hotel.hotel_name || 'Unknown',
 occupied,
 total,
 rate: Math.round((occupied / total) * 100)
 });
 }
 });

 setRooms(allRooms);

 // Room Types
 const typeCounts = {};
 allRooms.forEach(r => {
 const t = r.type || 'Standard';
 typeCounts[t] = (typeCounts[t] || 0) + 1;
 });
 const roomTypes = Object.entries(typeCounts).map(([name, value]) => ({ name, value }));

 setAccommodation({
 totalProperties: hotelsData.length,
 totalRooms: totalRoomsCount,
 occupiedRooms: occupiedRoomsCount,
 occupancyRate: totalRoomsCount > 0 ? Math.round((occupiedRoomsCount / totalRoomsCount) * 100) : 0,
 propertyOccupancy: propertyStats,
 roomTypes
 });

 } catch (error) {
 console.error("Failed to load analytics data:", error);
 } finally {
 setLoading(false);
 }
 };

 fetchData();
 }, []);

 // Define export columns
 const exportColumns = [
 { header: 'Full Name', key: 'full_name' },
 { header: 'Reference', key: 'home_office_reference' },
 { header: 'Date of Birth', key: 'date_of_birth' },
 { header: 'Nationality', key: 'nationality' },
 { header: 'Gender', key: 'gender' },
 { header: 'Status', key: 'status' },
 { header: 'Property', key: 'property_name' },
 { header: 'Room', key: 'room_number' }
 ];

 const prepareExportData = () => {
 return serviceUsers.map(u => {
 // Find property and room info if not directly on user object
 const prop = properties.find(p => p.id === u.property_id || p.id === u.hotel_id);
 const room = rooms.find(r => r.id === u.room_id);

 return {
 full_name: `${u.first_name || ''} ${u.last_name || ''}`.trim(),
 home_office_reference: u.home_office_reference || 'N/A',
 date_of_birth: u.date_of_birth || u.dob || '',
 nationality: u.nationality || 'N/A',
 gender: u.gender || 'N/A',
 status: u.status || 'Active',
 property_name: prop?.name || u.property_name || 'Unassigned',
 room_number: room?.room_number || u.room_number || 'Unassigned'
 };
 });
 };

 const exportToPDF = () => {
 try {
 const data = prepareExportData();
 generatePDF(data, exportColumns, 'Service User Analytics Report', 'service-user-analytics');
 } catch (error) {
 console.error('Export PDF failed:', error);
 alert('Failed to export PDF');
 }
 };

 const exportToExcel = () => {
 try {
 const data = prepareExportData();
 generateCSV(data, exportColumns, 'service-user-analytics');
 } catch (error) {
 console.error('Export CSV failed:', error);
 alert('Failed to export CSV');
 }
 };
 if (loading) {
 return (
 <div className="flex items-center justify-center min-h-screen">
 <div className="text-center">
 <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-indigo-600 mx-auto"></div>
 <p className="mt-4 text-gray-600">Loading analytics...</p>
 </div>
 </div>
 );
 }

 return (
 <div className="min-h-screen bg-gray-50 p-6">
 <div className="w-[90%] max-w-[1800px] mx-auto">
 {/* Header */}
 <div className="mb-8">
 <div className="flex items-center justify-between">
 <div>
 <h1 className="text-3xl font-bold text-gray-900 mb-2">Service User Analytics</h1>
 <p className="text-gray-600">Comprehensive analytics and reporting for service users</p>
 </div>
 {/* Export Buttons */}
 <div className="flex gap-3">
 <button
 onClick={exportToPDF}
 className="flex items-center gap-2 px-5 py-2.5 bg-red-500 text-white rounded-xl -xl hover:bg-red-600 transition-all shadow-lg font-medium"
 >
 <Download className="w-4 h-4" />
 Export PDF
 </button>
 <button
 onClick={exportToExcel}
 className="flex items-center gap-2 px-5 py-2.5 bg-green-500 text-white rounded-xl -xl hover:bg-green-600 transition-all shadow-lg font-medium"
 >
 <Download className="w-4 h-4" />
 Export Excel
 </button>
 </div>
 </div>
 </div>

 {/* Summary Cards */}
 <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
 <div className="bg-white rounded-xl p-5 shadow-sm border border-gray-100 flex items-center gap-4 ">
 <div className="bg-blue-100 text-blue-600 h-14 w-14 rounded-full flex items-center justify-center flex-shrink-0">
 <Users className="w-7 h-7" />
 </div>
 <div className="flex-1 min-w-0">
 <div className="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-0.5">Total Service Users</div>
 <div className="text-2xl font-black text-slate-800 leading-none">{demographics.totalUsers}</div>
 </div>
 </div>

 <div className="bg-white rounded-xl p-5 shadow-sm border border-gray-100 flex items-center gap-4 ">
 <div className="bg-purple-100 text-purple-600 h-14 w-14 rounded-full flex items-center justify-center flex-shrink-0">
 <Building2 className="w-7 h-7" />
 </div>
 <div className="flex-1 min-w-0">
 <div className="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-0.5">Total Properties</div>
 <div className="text-2xl font-black text-slate-800 leading-none">{accommodation.totalProperties}</div>
 </div>
 </div>

 <div className="bg-white rounded-xl p-5 shadow-sm border border-gray-100 flex items-center gap-4 ">
 <div className="bg-teal-100 text-teal-600 h-14 w-14 rounded-full flex items-center justify-center flex-shrink-0">
 <Bed className="w-7 h-7" />
 </div>
 <div className="flex-1 min-w-0">
 <div className="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-0.5">Occupancy Rate</div>
 <div className="text-2xl font-black text-slate-800 leading-none">{accommodation.occupancyRate}%</div>
 </div>
 </div>

 <div className="bg-white rounded-xl p-5 shadow-sm border border-gray-100 flex items-center gap-4 ">
 <div className="bg-orange-100 text-orange-600 h-14 w-14 rounded-full flex items-center justify-center flex-shrink-0">
 <TrendingUp className="w-7 h-7" />
 </div>
 <div className="flex-1 min-w-0">
 <div className="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-0.5">Moved Out</div>
 <div className="text-2xl font-black text-slate-800 leading-none">{demographics.movedOut}</div>
 </div>
 </div>
 </div>

 {/* Tabs */}
 <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-6">
 <div className="mb-6 flex items-center gap-3 border-b border-gray-200">
 <button
 onClick={() => setActiveTab('demographics')}
 className={`pb-3 px-1 text-sm font-medium border-b-2 transition-colors ${activeTab === 'demographics'
 ? 'border-teal-500 text-teal-600'
 : 'border-transparent text-gray-500 hover:text-gray-700'
 }`}
 >
 <div className="flex items-center gap-2">
 <PieChart className="w-4 h-4" />
 Demographics
 </div>
 </button>
 <button
 onClick={() => setActiveTab('accommodation')}
 className={`pb-3 px-1 text-sm font-medium border-b-2 transition-colors ${activeTab === 'accommodation'
 ? 'border-teal-500 text-teal-600'
 : 'border-transparent text-gray-500 hover:text-gray-700'
 }`}
 >
 <div className="flex items-center gap-2">
 <Home className="w-4 h-4" />
 Accommodation
 </div>
 </button>
 <button
 onClick={() => setActiveTab('reports')}
 className={`pb-3 px-1 text-sm font-medium border-b-2 transition-colors ${activeTab === 'reports'
 ? 'border-teal-500 text-teal-600'
 : 'border-transparent text-gray-500 hover:text-gray-700'
 }`}
 >
 <div className="flex items-center gap-2">
 <BarChart3 className="w-4 h-4" />
 Reports
 </div>
 </button>
 </div>

 {/* Demographics Tab */}
 {activeTab === 'demographics' && (
 <div className="space-y-6">
 {/* Age Distribution */}
 <div className="bg-gray-50 rounded-xl p-6">
 <h3 className="text-xl font-semibold text-gray-900 mb-4">Age Distribution</h3>
 <div className="space-y-3">
 {demographics.ageGroups.map((group) => (
 <div key={group.name} className="flex items-center gap-3">
 <div className="w-20 text-sm font-medium text-gray-700">{group.name}</div>
 <div className="flex-1">
 <div className="flex items-center gap-2">
 <div className="flex-1 bg-gray-200 rounded-full h-8 overflow-hidden">
 <div
 className="bg-indigo-600 h-full flex items-center justify-center text-white text-xs font-medium"
 style={{ width: `${(group.value / demographics.totalUsers) * 100}%` }}
 >
 {group.value > 0 && group.value}
 </div>
 </div>
 <div className="w-12 text-sm text-gray-600">
 {Math.round((group.value / demographics.totalUsers) * 100)}%
 </div>
 </div>
 </div>
 </div>
 ))}
 </div>
 </div>

 {/* Gender & Immigration Status */}
 <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
 {/* Gender Distribution */}
 <div className="bg-gray-50 rounded-xl p-6">
 <h3 className="text-xl font-semibold text-gray-900 mb-4">Gender Distribution</h3>
 <div className="space-y-3">
 {demographics.genderDistribution.map((item, idx) => (
 <div key={idx} className="flex items-center justify-between">
 <span className="text-sm font-medium text-gray-700">{item.name}</span>
 <div className="flex items-center gap-2">
 <div className="w-32 bg-gray-200 rounded-full h-6 overflow-hidden">
 <div
 className="bg-purple-600 h-full flex items-center justify-center text-white text-xs font-medium"
 style={{ width: `${(item.value / demographics.totalUsers) * 100}%` }}
 >
 {Math.round((item.value / demographics.totalUsers) * 100)}%
 </div>
 </div>
 <span className="text-sm font-medium text-gray-900 w-8">{item.value}</span>
 </div>
 </div>
 ))}
 </div>
 </div>

 {/* Immigration Status */}
 <div className="bg-gray-50 rounded-xl p-6">
 <h3 className="text-xl font-semibold text-gray-900 mb-4">Immigration Status</h3>
 <div className="space-y-3">
 {demographics.immigrationStatus.map((item, idx) => (
 <div key={idx} className="flex items-center justify-between">
 <span className="text-sm font-medium text-gray-700 truncate max-w-[150px]">{item.name}</span>
 <div className="flex items-center gap-2">
 <div className="w-32 bg-gray-200 rounded-full h-6 overflow-hidden">
 <div
 className="bg-teal-600 h-full flex items-center justify-center text-white text-xs font-medium"
 style={{ width: `${(item.value / demographics.totalUsers) * 100}%` }}
 >
 {Math.round((item.value / demographics.totalUsers) * 100)}%
 </div>
 </div>
 <span className="text-sm font-medium text-gray-900 w-8">{item.value}</span>
 </div>
 </div>
 ))}
 </div>
 </div>
 </div>

 {/* Top Nationalities */}
 <div className="bg-gray-50 rounded-xl p-6">
 <h3 className="text-xl font-semibold text-gray-900 mb-4">Top 10 Nationalities</h3>
 <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4">
 {demographics.nationalityDistribution.map((item, idx) => (
 <div key={idx} className="flex flex-col items-center justify-center p-4 bg-white rounded-xl shadow-sm ">
 <div className="text-2xl font-bold text-indigo-600 mb-1">{item.value}</div>
 <div className="text-xs text-center text-gray-600">{item.name}</div>
 </div>
 ))}
 </div>
 </div>
 </div>
 )}

 {/* Accommodation Tab */}
 {activeTab === 'accommodation' && (
 <div className="space-y-6">
 {/* Property Occupancy */}
 <div className="bg-gray-50 rounded-xl p-6">
 <h3 className="text-xl font-semibold text-gray-900 mb-4">Property Occupancy Rates</h3>
 <div className="space-y-4">
 {accommodation.propertyOccupancy.map((prop, idx) => (
 <div key={idx} className="border-b border-gray-200 pb-4 last:border-0">
 <div className="flex items-center justify-between mb-2">
 <span className="font-medium text-gray-900">{prop.name}</span>
 <span className="text-sm text-gray-600">{prop.occupied} / {prop.total} rooms</span>
 </div>
 <div className="flex items-center gap-2">
 <div className="flex-1 bg-gray-200 rounded-full h-8 overflow-hidden relative" style={{ minHeight: '32px' }}>
 <div
 className="bg-green-600 h-full flex items-center justify-center text-white text-xs font-medium"
 style={{ width: `${prop.rate}%` }}
 >
 {prop.rate}%
 </div>
 </div>
 </div>
 </div>
 ))}
 </div>
 </div>

 {/* Room Types and Summary */}
 <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
 {/* Room Types */}
 {accommodation.roomTypes.length > 0 && (
 <div className="bg-gray-50 rounded-xl p-6">
 <h3 className="text-xl font-semibold text-gray-900 mb-4">Room Types</h3>
 <div className="grid grid-cols-2 gap-3">
 {accommodation.roomTypes.map((type, idx) => (
 <div key={idx} className="p-4 bg-white rounded-xl shadow-sm text-center ">
 <div className="text-2xl font-bold text-indigo-600 mb-1">{type.value}</div>
 <div className="text-sm text-gray-600">{type.name}</div>
 </div>
 ))}
 </div>
 </div>
 )}

 {/* Summary Stats */}
 <div className="space-y-3">
 <div className="bg-gradient-to-br from-green-50 to-emerald-50 rounded-xl p-6 border border-green-100">
 <div className="flex items-center justify-between">
 <div>
 <p className="text-sm text-gray-600 mb-1">Occupied Rooms</p>
 <p className="text-2xl font-bold text-gray-900">{accommodation.occupiedRooms}</p>
 </div>
 <Bed className="w-8 h-8 text-green-500" />
 </div>
 </div>

 <div className="bg-gradient-to-br from-blue-50 to-cyan-50 rounded-xl p-6 border border-blue-100">
 <div className="flex items-center justify-between">
 <div>
 <p className="text-sm text-gray-600 mb-1">Available Rooms</p>
 <p className="text-2xl font-bold text-gray-900">{accommodation.totalRooms - accommodation.occupiedRooms}</p>
 </div>
 <Home className="w-8 h-8 text-blue-500" />
 </div>
 </div>

 <div className="bg-gradient-to-br from-purple-50 to-pink-50 rounded-xl p-6 border border-purple-100">
 <div className="flex items-center justify-between">
 <div>
 <p className="text-sm text-gray-600 mb-1">Occupancy Rate</p>
 <p className="text-2xl font-bold text-gray-900">{accommodation.occupancyRate}%</p>
 </div>
 <TrendingUp className="w-8 h-8 text-purple-500" />
 </div>
 </div>
 </div>
 </div>
 </div>
 )}

 {/* Reports Tab */}
 {activeTab === 'reports' && (
 <div className="space-y-6">
 <div className="bg-gray-50 rounded-xl p-6">
 <h3 className="text-xl font-semibold text-gray-900 mb-4">Available Reports</h3>
 <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
 <div className="p-4 bg-white border border-gray-200 rounded-xl hover:border-teal-400 transition-all cursor-pointer">
 <PieChart className="w-8 h-8 text-indigo-600 mb-3" />
 <h4 className="font-semibold text-gray-900 mb-2">Demographics Report</h4>
 <p className="text-sm text-gray-600 mb-3">Comprehensive breakdown of service user demographics.</p>
 <button className="rounded-xl text-sm text-indigo-600 hover:text-indigo-700 font-medium">
 Generate Report →
 </button>
 </div>

 <div className="p-4 bg-white border border-gray-200 rounded-xl hover:border-teal-400 transition-all cursor-pointer">
 <PieChart className="w-8 h-8 text-indigo-600 mb-3" />
 <h4 className="font-semibold text-gray-900 mb-2">Demographics Report</h4>
 <p className="text-sm text-gray-600 mb-3">Comprehensive breakdown of service user demographics.</p>
 <button className="rounded-xl text-sm text-indigo-600 hover:text-indigo-700 font-medium">
 Generate Report →
 </button>
 </div>

 <div className="p-4 bg-white border border-gray-200 rounded-xl hover:border-teal-400 transition-all cursor-pointer">
 <Home className="w-8 h-8 text-teal-600 mb-3" />
 <h4 className="font-semibold text-gray-900 mb-2">Accommodation Report</h4>
 <p className="text-sm text-gray-600 mb-3">Detailed occupancy statistics and property performance.</p>
 <button className="rounded-xl text-sm text-teal-600 hover:text-teal-700 font-medium">
 Generate Report →
 </button>
 </div>

 <div className="p-4 bg-white border border-gray-200 rounded-xl hover:border-blue-400 transition-all cursor-pointer">
 <Users className="w-8 h-8 text-blue-600 mb-3" />
 <h4 className="font-semibold text-gray-900 mb-2">Service User Summary</h4>
 <p className="text-sm text-gray-600 mb-3">Complete list of active service users with details.</p>
 <button className="rounded-xl text-sm text-blue-600 hover:text-blue-700 font-medium">
 Generate Report →
 </button>
 </div>

 <div className="p-4 bg-white border border-gray-200 rounded-xl hover:border-purple-400 transition-all cursor-pointer">
 <TrendingUp className="w-8 h-8 text-purple-600 mb-3" />
 <h4 className="font-semibold text-gray-900 mb-2">Move In/Out Report</h4>
 <p className="text-sm text-gray-600 mb-3">Historical data on service user admissions and departures.</p>
 <button className="rounded-xl text-sm text-purple-600 hover:text-purple-700 font-medium">
 Generate Report →
 </button>
 </div>

 <div className="p-4 bg-white border border-gray-200 rounded-xl hover:border-orange-400 transition-all cursor-pointer">
 <Calendar className="w-8 h-8 text-orange-600 mb-3" />
 <h4 className="font-semibold text-gray-900 mb-2">Vulnerability Assessment</h4>
 <p className="text-sm text-gray-600 mb-3">Analysis of vulnerabilities across the population.</p>
 <button className="rounded-xl text-sm text-orange-600 hover:text-orange-700 font-medium">
 Generate Report →
 </button>
 </div>

 <div className="p-4 bg-white border border-gray-200 rounded-xl hover:border-pink-400 transition-all cursor-pointer">
 <BarChart3 className="w-8 h-8 text-pink-600 mb-3" />
 <h4 className="font-semibold text-gray-900 mb-2">Custom Report</h4>
 <p className="text-sm text-gray-600 mb-3">Build your own custom report with selected fields.</p>
 <button className="rounded-xl text-sm text-pink-600 hover:text-pink-700 font-medium">
 Generate Report →
 </button>
 </div>
 </div>
 </div>

 {/* Quick Stats Table */}
 <div className="bg-gray-50 rounded-xl p-6">
 <h3 className="text-xl font-semibold text-gray-900 mb-4">Quick Statistics</h3>
 <div className="overflow-x-auto">
 <table className="w-full">
 <thead>
 <tr className="border-b-2 border-gray-300">
 <th className="text-left py-3 px-4 text-sm font-semibold text-gray-700">Metric</th>
 <th className="text-right py-3 px-4 text-sm font-semibold text-gray-700">Value</th>
 <th className="text-right py-3 px-4 text-sm font-semibold text-gray-700">Percentage</th>
 </tr>
 </thead>
 <tbody className="bg-white">
 <tr className="border-b border-gray-100 hover:bg-gray-50 transition-colors">
 <td className="py-3 px-4 text-sm text-gray-900">Total Service Users</td>
 <td className="py-3 px-4 text-sm text-gray-900 text-right font-medium">{demographics.totalUsers}</td>
 <td className="py-3 px-4 text-sm text-gray-600 text-right">100%</td>
 </tr>
 <tr className="border-b border-gray-100 hover:bg-gray-50 transition-colors">
 <td className="py-3 px-4 text-sm text-gray-900">Active Users</td>
 <td className="py-3 px-4 text-sm text-gray-900 text-right font-medium">{demographics.activeUsers}</td>
 <td className="py-3 px-4 text-sm text-gray-600 text-right">
 {demographics.totalUsers > 0 ? Math.round((demographics.activeUsers / demographics.totalUsers) * 100) : 0}%
 </td>
 </tr>
 <tr className="border-b border-gray-100 hover:bg-gray-50 transition-colors">
 <td className="py-3 px-4 text-sm text-gray-900">Moved Out</td>
 <td className="py-3 px-4 text-sm text-gray-900 text-right font-medium">{demographics.movedOut}</td>
 <td className="py-3 px-4 text-sm text-gray-600 text-right">
 {demographics.totalUsers > 0 ? Math.round((demographics.movedOut / demographics.totalUsers) * 100) : 0}%
 </td>
 </tr>
 <tr className="border-b border-gray-100 hover:bg-gray-50 transition-colors">
 <td className="py-3 px-4 text-sm text-gray-900">Total Properties</td>
 <td className="py-3 px-4 text-sm text-gray-900 text-right font-medium">{accommodation.totalProperties}</td>
 <td className="py-3 px-4 text-sm text-gray-600 text-right">-</td>
 </tr>
 <tr className="border-b border-gray-100 hover:bg-gray-50 transition-colors">
 <td className="py-3 px-4 text-sm text-gray-900">Total Rooms</td>
 <td className="py-3 px-4 text-sm text-gray-900 text-right font-medium">{accommodation.totalRooms}</td>
 <td className="py-3 px-4 text-sm text-gray-600 text-right">-</td>
 </tr>
 <tr className="hover:bg-gray-50 transition-colors">
 <td className="py-3 px-4 text-sm text-gray-900">Occupied Rooms</td>
 <td className="py-3 px-4 text-sm text-gray-900 text-right font-medium">{accommodation.occupiedRooms}</td>
 <td className="py-3 px-4 text-sm text-gray-600 text-right">{accommodation.occupancyRate}%</td>
 </tr>
 </tbody>
 </table>
 </div>
 </div>
 </div>
 )}
 </div>
 </div>
 </div>
 );
}
