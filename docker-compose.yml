version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: hr-crm-db
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres_password}
      POSTGRES_DB: ${DB_NAME:-hr_crm}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - hr-network
    restart: always

  hr-backend:
    build:
      context: .
      target: backend
    container_name: hr-crm-backend
    environment:
      - DATABASE_URL=postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres_password}@db:5432/${DB_NAME:-hr_crm}
      - JWT_SECRET=${JWT_SECRET:-secret}
      - PORT=4000
      - NODE_ENV=production
      - CORS_ORIGINS=http://localhost:3002
    depends_on:
      - db
    ports:
      - "4000:4000"
    networks:
      - hr-network
    restart: always
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  hr-frontend:
    build:
      context: .
      target: frontend
    container_name: hr-crm-frontend
    ports:
      - "3002:80"
    depends_on:
      hr-backend:
        condition: service_started
    networks:
      - hr-network
    restart: always

networks:
  hr-network:
    driver: bridge

volumes:
  postgres_data:
