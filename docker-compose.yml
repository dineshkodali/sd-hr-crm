services:
  # Using remote database - local db service removed

  backend:
    build:
      context: .
      target: backend
    container_name: crm-backend
    env_file:
      - ./Backend/.env
    environment:
      - JWT_SECRET=${JWT_SECRET:-your-super-secret-jwt-key-change-this-in-production}
      - PORT=4000
      - NODE_ENV=production
      - CORS_ORIGINS=https://crm.sdcsolutions.in,http://crm.sdcsolutions.in,http://crm.sdgsolutions.in,https://crm.sdgsolutions.in,http://localhost:3002,http://localhost:3000
      - DATABASE_URL=postgresql://sdcommercial:sdcommercial@18.130.77.174:5432/new_CRM
      - DB_HOST=18.130.77.174
      - DB_PORT=5432
      - DB_NAME=new_CRM
      - DB_USER=sdcommercial
      - DB_PASSWORD=sdcommercial
    ports:
      - "4000:4000"
    networks:
      - crm-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build:
      context: .
      target: frontend
    container_name: crm-frontend
    ports:
      - "3002:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - crm-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  crm-network:
    driver: bridge
